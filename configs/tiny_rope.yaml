seed: 0
device: cuda

data:
  task: add
  max_digits: 3
  fixed_width: true
  train_size: 50000
  val_size: 2000
  max_seq_len: 24

model:
  vocab_size: null
  d_model: 128
  n_layers: 2
  n_heads: 4
  d_ff: 512
  dropout: 0.1
  max_seq_len: 24
  pos_encoding: rope

train:
  batch_size: 64
  lr: 0.0003
  steps: 3000
  eval_every: 200
  out_dir: runs
